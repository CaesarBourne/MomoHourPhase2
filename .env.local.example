# Default GHA base URL the portal talks to on first load. Can be changed at
# runtime from the Settings page (stored in the browser, no rebuild needed) -
# this is just the fallback before any override is set.
NEXT_PUBLIC_GHA_BASE_URL=http://localhost:3000
